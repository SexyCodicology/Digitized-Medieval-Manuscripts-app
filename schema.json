{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Medieval Manuscripts Database",
  "description": "Schema for cataloging medieval manuscript collections",
  "type": "array",
  "items": {
    "type": "object",
    "additionalProperties": false,
    "required": ["id", "library", "nation", "city", "website", "copyright", "licence_category", "quantity", "iiif", "is_free_cultural_works_license", "aggregators"],
    "properties": {
      "id": {
        "type": "integer",
        "minimum": 1,
        "description": "Unique identifier for the record"
      },
      "isil": {
        "type": "string",
        "pattern": "^[A-Z]{2}-[A-Za-z0-9][A-Za-z0-9._-]*$",
        "description": "Optional International Standard Identifier for Libraries in CountryCode-LocalCode form, for example GB-OxBodl."
      },
      "wikidata_qid": {
        "type": "string",
        "pattern": "^Q[1-9][0-9]*$",
        "description": "Optional Wikidata entity identifier, for example Q1131283."
      },
      "geonames_id": {
        "type": "integer",
        "minimum": 1,
        "description": "Optional GeoNames numeric identifier for the library location, for example 2640729."
      },
      "library": {
        "type": "string",
        "minLength": 2,
        "description": "Name of the library or institution"
      },
      "nation": {
        "type": "string",
        "description": "Country where the library is located"
      },
      "city": {
        "type": "string",
        "description": "City where the library is located"
      },
      "website": {
        "type": "string",
        "format": "uri",
        "pattern": "^https?://",
        "description": "URL to the library's manuscript collection; must use HTTP or HTTPS"
      },
      "copyright": {
        "type": "string",
        "description": "Verbatim copyright or licence information as stated by the institution"
      },
      "licence_category": {
        "type": "string",
        "enum": ["CC0", "CC-BY", "CC-BY-NC", "CC-BY-NC-SA", "CC-BY-NC-ND", "All Rights Reserved", "Mixed/Item-specific", "Unknown"],
        "description": "Normalised rights category used for filtering; copyright retains the institution's verbatim wording"
      },
      "quantity": {
        "type": "string",
        "enum": ["Few", "Dozens", "Hundreds", "Thousands", "Unknown"],
        "description": "Approximate number of digitized medieval manuscripts"
      },
      "iiif": {
        "type": "boolean",
        "description": "Whether the collection supports IIIF (International Image Interoperability Framework)"
      },
      "iiif_collection_url": {
        "type": "string",
        "format": "uri",
        "pattern": "^https?://",
        "description": "Optional IIIF Presentation API collection JSON endpoint. Use only when its scope matches the listed medieval manuscript collection; do not use a viewer URL. Only meaningful when iiif is true."
      },
      "iiif_example_manifest_url": {
        "type": "string",
        "format": "uri",
        "pattern": "^https?://",
        "description": "Optional direct IIIF Presentation API manifest JSON endpoint for a representative manuscript. Requires iiif_example_manifest_label; do not use a viewer URL. Only meaningful when iiif is true."
      },
      "iiif_example_manifest_label": {
        "type": "string",
        "minLength": 1,
        "description": "Required descriptive label when iiif_example_manifest_url is set. Name the representative manuscript so readers understand it is an example."
      },
      "is_free_cultural_works_license": {
        "type": "boolean",
        "description": "Whether the collection uses a Creative Commons free cultural works license"
      },
      "aggregators": {
        "type": "array",
        "description": "Aggregator projects or websites this collection is discoverable through, if any. Use an empty array for a collection with no aggregator membership.",
        "items": {
          "type": "object",
          "additionalProperties": false,
          "required": ["name", "url"],
          "properties": {
            "name": {
              "type": "string",
              "minLength": 1,
              "description": "Name of the aggregator project or website"
            },
            "url": {
              "type": "string",
              "format": "uri",
              "pattern": "^https?://",
              "description": "Canonical home page of the aggregator; must use HTTP or HTTPS"
            }
          }
        }
      },
      "is_disabled": {
        "type": "boolean",
        "description": "Whether the collection URL has been confirmed unreachable. Optional: omit it for a collection whose link is believed to work."
      },
      "last_checked": {
        "type": "string",
        "format": "date",
        "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
        "description": "ISO 8601 calendar date (YYYY-MM-DD) the collection URL was last assessed. It shows when a working link was last confirmed, or when a broken link was last confirmed. Required when is_disabled is true, so a warning shown to a reader can always be dated."
      },
      "added": {
        "type": "string",
        "format": "date",
        "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
        "description": "Optional ISO 8601 calendar date (YYYY-MM-DD) the record was added to DMMapp."
      },
      "last_edited": {
        "type": "string",
        "format": "date",
        "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
        "description": "Optional ISO 8601 calendar date (YYYY-MM-DD) the record was last materially edited."
      }
    }
  }
}
