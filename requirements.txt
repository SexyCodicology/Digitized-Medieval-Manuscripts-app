# MkDocs Material Theme for Documentation
# Used by GitHub Actions to build the documentation site
# [imaging] pulls in Pillow + CairoSVG, needed by the social plugin to
# render per-page share card images.
mkdocs-material[imaging]>=9.7.7
mkdocs-rss-plugin>=1.19.0

