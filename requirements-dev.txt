# Development dependencies — used by CI and local test runs only.
# The site itself builds with requirements.txt alone.
-r requirements.txt

# Test runner for the build-time hooks in hooks/
pytest>=9.1.1

# Schema validation for docs/assets/data.json, used by scripts/validate_data.py
jsonschema>=4.26.0,<5
